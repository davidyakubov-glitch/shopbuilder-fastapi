"""Baseline migration environment placeholder for the milestone repository.

The repo includes manual baseline migration artifacts for both platform and tenant
schemas under migrations/versions. Runtime startup also creates tables so the
project stays runnable with only `docker compose up` during this sprint.
"""
